name: Agent delivery policy

on:
  pull_request:
    types: [opened, edited, reopened, synchronize, ready_for_review]

permissions:
  contents: read
  pull-requests: read

jobs:
  validate:
    name: Validate agent delivery policy
    runs-on: ubuntu-latest
    steps:
      - uses: Plaintext-Lab/agentic-workflows/.github/actions/pr-policy@65a5cde3730aef6a9afd5a84fc94c6c04e4b5399
